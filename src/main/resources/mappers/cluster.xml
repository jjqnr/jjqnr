<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.common.dao.IClusterDao">
    <select id="getClusterList"  statementType="STATEMENT" resultType="com.common.dao.Cluster" parameterType="String">
        select * from ${database}.cluster limit 500
    </select>
</mapper>